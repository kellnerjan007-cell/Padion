Read `docs/plan.md` and show the next uncompleted task.

Find the first line that starts with `- [ ]` (unchecked checkbox).

Show:
1. **Which Phase and Section** the task belongs to (e.g., "Phase 1 – Foundation & MVP → 1.3 Type Definitions")
2. **The task** itself
3. **The full Acceptance Criteria** for that section (the bold "Acceptance:" line)
4. **All other unchecked tasks in the same section** so you have context

Then suggest the best approach to complete this task given the Padion architecture (referencing docs/architecture.md patterns where relevant).
